# Web 正式环境必须配置为部署站点上的同源 WebDAV 代理路径。
# 代理只能转发到 https://dav.jianguoyun.com/dav/，且不得记录 Authorization。
VITE_WEBDAV_PROXY_URL=/api/webdav

# 仅填写非敏感默认值；不要在环境文件中保存 WebDAV 应用密码。
VITE_DEFAULT_WEBDAV_USERNAME=
