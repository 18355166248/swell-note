# 笔记编辑优化推进记录

更新时间：2026-09-06。工作目录：`/Users/xmly/Swell/code/swell-note`。
基线：`main@0df66ac`。用户已授权按审计方案依次实现 A/B/C 三批，先落本地记录防止中断丢失。
审计和复现材料见同目录 `README.md`。不改已有用户笔记，不执行云同步。

## 状态与验收

- [x] A/F01：正文和表格活动编辑目标统一，单元格格式不改错正文，一次撤销恢复。
- [x] A/F02：阅读/编辑切换以及重命名保留笔记编辑会话、选区和撤销历史。
- [x] A/F03：桌面/手机表格行列、水平、垂直菜单可见可操作，处理边界和焦点。
- [x] A/F04：图片/块插入识别表格边界，避免并入表格。
- [x] A/F05：新建聚焦标题，实际重命名确认后连续进入正文。
- [x] B/F06：阅读态数值列对齐与编辑态一致。
- [x] B/F07：TSV/HTML 多格粘贴，扩展网格和整次撤销。
- [x] B/F08：深色代码、脚注、选区和高亮可读。
- [x] B/F10：图片加载失败可重试、看来源、定位/更换。
- [x] B/F11：放大层关闭按钮、焦点约束、Esc 和返回焦点。
- [x] C/F09：图片查看、尺寸、更换、删除轻量工具及合理默认尺寸。
- [x] C/F12：公式与 Mermaid 按需渲染，错误可回退源码。
- [x] C/F13：属性/提示块/高亮/脚注编辑与阅读视觉语义收敛。
- [x] C/F14：撤销重做可用状态、统一标题等级、触控目标。
- [x] C/F15：重命名文案、表格摘要、脚注中文化。
- [x] 风险 R01–R03：表格 IME 守卫、异步附件插入位置、剪切选区竞态。
- [x] 完成相关单测、TypeScript、生产构建和桌面/手机浏览器验证。

## 执行日志

- A 已落地：活动单元格独立选区、内存编辑会话（最近 20 篇）、表格 top-layer 浮层及旧浏览器降级、表格后插图边界、新建标题聚焦。
- 重命名实测发现手机详情层也按路径重建，并且路由过渡会清空 activeNote；已统一稳定渲染身份并保护重命名过渡。已验证标题回车后连续输入正文、再次重命名后一次撤销恢复正文。
- B 已落地：阅读态表格列对齐、TSV/HTML 二维粘贴和单次撤销、深色 CodeMirror 主题、图片重试/来源入口、Radix 图片对话框。已实测 Tab 不离开对话框、Esc 返回原图。
- C 已落地：图片查看、240/480/720/自适应宽度、替换地址/附件路径、删除及撤销；KaTeX 和 Mermaid 动态加载；frontmatter/提示块/高亮/脚注样式；统一标题下拉、按钮状态、手机触控尺寸；摘要去管道符、换行标记和高亮标记，脚注与重命名文案中文化。
- R01–R03：单元格 IME 确认键守卫，附件上传书签按文档事务映射，异步剪切/粘贴等待期间核验文档和输入选区。
- 回归：`pnpm test` 453 通过，1 项 live WebDAV 集成测试按默认配置跳过；TypeScript 通过；生产构建通过（仍有包体积提示，公式/流程图均为动态分块）。桌面 1280×800、手机 390×844 已浏览器实测。
- 新增本地离线样本：`优化回归验证-最终（测试）`。已验证真实插图在表格外、尺寸在读写两态均为 240px、删除一次撤销恢复、列菜单 hit-test 命中。新增截图 18/19/20。错误 Mermaid 已实测显示错误提示及源码，错误公式保留可读错误表达式，页面其余内容仍正常。

## 已知边界

- 编辑器撤销会话属于本次应用运行的内存，不承诺刷新/重启后撤销；外部正文已改变时主动放弃旧撤销栈。
- 图片“更换”支持地址或已有附件路径；新文件继续使用插入附件入口。这里不实现图片裁剪或完整媒体管理。
- TSV/HTML 按网格文本粘贴，不复制源表格样式、合并单元格语义或公式计算。
- 公式与 Mermaid 在阅读态渲染；编辑态保留可编辑源码。真机中文输入法、iOS 软键盘和在线同步未执行，本轮不触碰云端数据。
- 浏览器验证走 Codex CUA；已更新原有端到端测试的标题选择器，但未运行 Playwright CLI 套件。


- 初始化：确认工作区只有上一轮审计目录未跟踪，应用代码干净。开始分析活动编辑目标、编辑会话和表格浮层实现。

## 中断后继续

1. 先读本记录与 `git diff --stat`；不要重做已验证项。
2. 所有变更保持在工作区；仅用户明确要求提交/推送时执行对应 Git 动作。
3. 最终记录实际测试结果和未能验证的真机/网络条件，不将源码检查当作真机通过。
