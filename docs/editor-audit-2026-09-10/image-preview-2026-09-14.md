# 图片预览与粘贴光标优化（2026-09-14）

## 评审与实现

按用户要求执行 GPT-6 Astra 首评 → GPT-5.6 Sol 实现 → Astra 复审 → GPT-5.6 补齐焦点边界 → Astra 最终复审。最终未发现阻断问题。

截图中的蓝色竖线不能单凭静态图认定为卡死。浏览器验证中，粘贴图片后只有一个 CodeMirror 光标，原生 caret 为透明，正文可以继续输入中文和英文；失焦时光标隐藏。没有通过隐藏正常输入光标规避问题。

## 改动

- 修复“编辑引用”选中整段但无法还原源码的问题，改为折叠光标后进入源码编辑。
- 图片本体支持点击、Enter、Space 打开预览；Esc 关闭后归还入口焦点。入口卸载时仅回退到仍连接的原编辑器。
- 区分加载、成功、失败状态，“重试”仅失败时显示；异步加载后请求 CodeMirror 重新测量。
- 尺寸、更换、删除、取消及键盘重试后恢复合理焦点。
- 独立审计页接入生产附件 writer 和内存存储，补齐生产的编辑器样式容器。

## 验证

- 相关 Vitest：101/101 通过；全量：761 通过，1 跳过。
- TypeScript 检查、`git diff --check`、`pnpm build` 通过。构建仍提示已有的 CSS `::highlight` 识别和较大分块警告。
- CUA 浏览器实际图片剪贴板粘贴 → 附件引用生成 → 图片显示 → 继续中文/英文输入：通过；生产样式下只有一个活动光标。
- 宽图点击及 Enter/Space 预览、Esc 返回图片入口：通过。
- 编辑引用显示源码：通过；240 宽度实际计算为 240px：通过。
- 更换为失效附件显示失败与重试，键盘重试后焦点留在正文：通过。
- 更换撤销、删除及一步撤销恢复图片：通过。

浏览器验收使用独立内存审计页与生产编辑器/附件逻辑，不写入用户笔记。未重新安装或替换 macOS 应用；原生 WebView 的系统截图粘贴尚未在本轮实机验收。
